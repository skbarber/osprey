
## Channel Database Terminology

### Database Profile

This is a **Middle Layer (MML)** channel database — channels are organized by
functional grouping: Systems → Families → Fields → Subfields → ChannelNames.
Navigation follows the MML convention used in many accelerator control systems.

### Terminology Mapping

Operators use domain language.
{% if facility_vocabulary %}
The device rows below are this deployment's own vocabulary, read from the
compiled ontology it declares (`facility.ontology`) — the same table the
channel database was generated from, so the family tokens are the ones this
facility actually uses:
{% else %}
No facility ontology is declared (`facility.ontology`), so the table below
carries only navigation guidance and no device vocabulary — match on names and
descriptions in the database and verify with a lookup rather than guessing.
{% endif %}
| Operator Language | MML Navigation |
|---|---|
{%- for entry in facility_vocabulary | default(none) or [] %}
| {{ '"' ~ (entry.synonyms | join('", "')) ~ '"' }} | {% if entry.families %}Family: {{ '`' ~ (entry.families | join('` or `')) ~ '`' }}{% else %}Umbrella class {{ entry.class_name }} — no family of its own; narrow to a specific family first{% endif %} |
{%- endfor %}
| "readback" / "monitor" | Navigate to system/family first, then select `Monitor` field |
| "setpoint" / "control" | Navigate to system/family first, then select `Setpoint` field |
| "storage ring" / "booster" / "transport line" | System: this facility's own abbreviation for that machine — read the descriptions `list_systems()` returns |
| "device number N" / "the fifth one" | Use the `devices` parameter to filter specific device instances |
| "common name" | Call `get_common_names(system, family)` for human-friendly device names |

### Middle Layer Conventions

- Always start with `list_systems()` to discover available systems and their descriptions
- System abbreviations are facility-specific — read the descriptions rather than guessing
- Family names follow accelerator conventions; `list_families()` on a system is the
  authority for which ones this database actually holds
- The `Monitor` field typically contains readback/measurement values
- The `Setpoint` field typically contains control/command values
- Subfields provide axis or component decomposition (e.g., `X`/`Y` for position monitors)
- Use `inspect_fields()` to understand the field/subfield structure before assuming
- When descriptions are available at any level, read them — they disambiguate similar-looking families
- `get_common_names()` maps between device instance numbers and human-friendly identifiers
