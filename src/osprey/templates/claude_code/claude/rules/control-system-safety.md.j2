{% set cs_type = control_system_type | default('mock') %}
{% if cs_type == 'epics' %}
{% set protocol_name = 'EPICS' %}
{% set protocol_long = 'EPICS Channel Access' %}
{% elif cs_type == 'tango' %}
{% set protocol_name = 'Tango' %}
{% set protocol_long = 'Tango Controls' %}
{% elif cs_type == 'opcua' %}
{% set protocol_name = 'OPC-UA' %}
{% set protocol_long = 'OPC-UA' %}
{% elif cs_type == 'labview' %}
{% set protocol_name = 'LabVIEW' %}
{% set protocol_long = 'LabVIEW' %}
{% else %}
{% set protocol_name = 'Control System' %}
{% set protocol_long = 'Control System' %}
{% endif %}
# Control System Safety — {{ protocol_long }}

When writing Python code that interacts with the control system, you MUST use
the `osprey.runtime` API. Direct use of {{ protocol_name }} libraries is **prohibited** because
it bypasses safety layers (limits checking, write confirmation, approval).

### Allowed

```python
from osprey.runtime import read_channel, write_channel
value = read_channel("SR:DIAG:BPM:01:POSITION:X")
write_channel("SR:MAG:QF:01:CURRENT:SP", 150.0)
```

### Prohibited

{% if cs_type in ('epics', 'virtual_accelerator') %}
```python
# DO NOT use these — they bypass safety controls
import epics
epics.caget("SR:DIAG:BPM:01:POSITION:X")   # Bypasses audit logging
epics.caput("SR:MAG:QF:01:CURRENT:SP", 150)  # Bypasses limits + approval
from epics import PV
pv = PV("SR:MAG:QF:01:CURRENT:SP")
pv.put(150)  # Bypasses all safety layers
```

The same prohibition covers pvAccess (PVA) through the `p4p` library — every
client flavor, not just the threaded one:

```python
# DO NOT use these — they bypass safety controls
from p4p.client.thread import Context      # Bypasses all safety layers
from p4p.client.asyncio import Context     # Same bypass, async client flavor
ctxt = Context("pva")
ctxt.get("SR:CAM:01:IMAGE")                 # Bypasses audit logging
ctxt.put("SR:MAG:QF:01:CURRENT:SP", 150)    # Bypasses limits + approval
ctxt.rpc("SR:SVC:ORBIT", request)           # Not approvable — refused at runtime
```

`ctxt.rpc(...)` is the one to remember: an rpc call carries an arbitrary
payload, so limits checking cannot apply to it — there is nothing for the
approval workflow to check. It is refused at runtime in every readonly run
and wherever limits checking is enabled. Use `write_channel` for the write
you actually need.
{% elif cs_type == 'tango' %}
```python
# DO NOT use these — they bypass safety controls
import tango
device = tango.DeviceProxy("sys/tg_test/1")
device.read_attribute("Position")     # Bypasses audit logging
device.write_attribute("Current", 150)  # Bypasses limits + approval
from PyTango import DeviceProxy
dev = DeviceProxy("sys/tg_test/1")
dev.write_attribute("Setpoint", 150)  # Bypasses all safety layers
```
{% elif cs_type == 'opcua' %}
```python
# DO NOT use these — they bypass safety controls
from opcua import Client
client = Client("opc.tcp://localhost:4840")
client.connect()
node = client.get_node("ns=2;i=2")
node.get_value()     # Bypasses audit logging
node.set_value(150)  # Bypasses limits + approval
```
{% elif cs_type == 'labview' %}
```python
# DO NOT use these — they bypass safety controls
import labview
labview.set_control("Current", 150)   # Bypasses limits + approval
# Any direct LabVIEW library calls bypass all safety layers
```
{% else %}
```python
# DO NOT use any direct hardware library calls — they bypass safety controls
# All reads and writes must go through osprey.runtime
```
{% endif %}

### Why This Matters

The `osprey.runtime` API enforces:
1. **Channel limits** — prevents out-of-range writes (channel_limits.json)
2. **Write confirmation** — re-reads the channel to confirm it holds the value sent
3. **Human approval** — routes writes through the approval workflow
4. **Audit logging** — records all operations for traceability

Direct {{ protocol_name }} calls skip all of these protections.

{% if target_switch_enabled | default(false) %}## Which Machine You Are Talking To

OSPREY names three machines, and a deployment has the ones its config
describes:

- **live** — the real accelerator, the machine the facility authored. A write
  here moves real hardware.
- **va** — the virtual accelerator, a simulation that answers the same channel
  names. A write here moves nothing real.
- **standin** — the live stand-in: a soft IOC this deployment runs for itself.
  It carries a real machine's posture — the same limits, the same approval
  prompts — because it is operated as one, not because a write reaches the
  facility. It shows as **LIVE MACHINE**, marked **(stand-in)** only while
  this deployment has actually stood the soft IOC up; read the label the
  roster gives you rather than assuming which form you will see.

One of them is active at a time, and every approval prompt names the active
target, so the operator sees which machine a write would land on before they
approve it. Read that line rather than assuming a default — a session may
already have been switched, and the channel names look the same either way.

`control_target` reports the roster: which of the three this deployment
actually has, which one is active, and whether another can be switched to right
now. A machine absent from the roster is one this deployment does not run. It
is the authority on availability. Do not infer it from a config file, from an
endpoint seen earlier, or from a channel that happened to answer.

`control_target_set` is the only way to change targets, and it is approval-gated
like any other write, so the operator decides. Nothing else moves a session
between machines — not an environment variable, not a config edit, not a
restart. When a request needs the other machine, ask for it with
`control_target_set` and say why.

Channels the deny list refuses stay refused on every target. That list says
which channels this deployment will never write, not which machine is active,
so switching to the virtual accelerator or the stand-in does not unlock a
denied channel — and switching in order to reach one is not a workaround, it
is the same refusal one step later.

{% endif %}## Write Operations

For scripts that write to the control system, use the `execute` MCP tool directly
with `execution_mode: "readwrite"` — the `execute` tool defaults to readonly
mode, which blocks write operations. Those are the only two accepted values;
any other spelling is rejected before the script runs.

A readonly run also refuses the indirect routes to the control system: starting
a process (so a shelled-out `caput` fails) and loading a shared library through
`ctypes`. This applies even when the intent is unrelated to hardware, so a
readonly script cannot shell out at all. Every refusal is reported to the
operator and recorded in the deployment's audit log, so working around one is
never the right move — resubmit with `execution_mode: "readwrite"` and let the
operator approve it.

## Choosing the Right Tool

Match the request to one tool and stop there. Reaching past the tool the request
calls for is how a single read turns into a script and a single write turns into
a sequence nobody approved.

- **One live value** — use `channel_read`. No script is needed.
- **One live write the operator asked for by name** — use `channel_write`.
- **Python analysis over data you already retrieved** — use `execute`. It needs
  no control-system access.
- **Python that must touch the control system** — use `execute` and reach the
  machine through the `osprey.runtime` API, never a protocol library directly.
{%- if "bluesky" in enabled_servers | default([]) %}
- **Multi-setting measurements** — use the Bluesky queue. Anything that sets the
  machine to more than one value is a plan; see below.
{%- else %}
- **Multi-setting measurements** — no queue is configured in this deployment. Do
  not stand in for one with a loop of `channel_write` calls or an `execute`
  script that steps the setpoint — tell the operator what the measurement
  would require.
{%- endif %}

The two write paths handle a write the machine did not confirm differently:
`osprey.runtime.write_channel` raises, while `channel_write` reports it in the
result's `outcome`.
{% if "bluesky" in enabled_servers | default([]) %}

## Measurements Go Through the Queue, Not Through Repeated Writes

A request that needs the machine set to **more than one value** is a plan, and
plans run through the Bluesky queue. This holds however the request was worded:
"step a corrector across a few settings and record where the beam sits",
"sweep this setpoint and read the response back", and "run a scan" are the same
operation, and none of them is a loop of `channel_write` calls.

Stepping a setpoint by hand instead costs the operator one approval for every
write rather than one for the whole measurement, produces no run — so nothing
reaches the queue panel, the data store, or `get_run_data` — and parks the
setpoint wherever the last accepted write left it if the sequence stops part
way. Use the `operating-bluesky-plans` skill; it owns this path.

`channel_read` is unrestricted, and a single `channel_write` the operator asked
for by name is a legitimate operation. It is a *sequence* of writes standing in
for a plan that is not.

## Starting a Bluesky Queue

Starting a queue moves accelerator hardware. Arming a start needs the lane's
launch token, which a persona is granted per lane: only when its config arms
writes for the machine that lane drives **and** runs the `bluesky` MCP server.
Write posture is per connector type — `control_system.writes_enabled` is what a
type inherits, and a `control_system.connector.<type>.writes_enabled` block
answers for that type instead — so a deployment armed for the simulator alone
holds a token for the simulator's lane and none for the live one. A read-only
persona never holds one at all.

Where it is granted, one approval is the whole gate: approving `queue_start` in
chat starts the queue. There is no second confirmation step anywhere else.

A deploy refuses to grant this token to a persona whose settings permit `Bash`.
The approval gates the `queue_start` tool, not a shell — an agent with a shell
could read the token out of its own environment and arm a queue with no approval
at all.

If `queue_start` is refused with `launch_token_required`, this agent cannot arm
the queue here — it was not granted a token, the token it holds does not match
the bridge's, or this deployment has no launch token configured at all. Do not
go looking for the token in the environment, in config, or through a shell. Tell
the operator what you have staged and that the
start was refused for a missing or mismatched token, and let them start the
queue from the BLUESKY panel.
{% endif -%}
