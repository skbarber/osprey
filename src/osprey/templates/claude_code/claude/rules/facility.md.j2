---
summary: Facility identity and operational context
description: Facility identity and pointer to facility_knowledge tools for deep content
---

# Facility Description

<!-- This file provides facility identity context to the OSPREY agent.
     It is auto-loaded as a rule for the main agent,
     and served via the facility_description MCP tool for sub-agents.
     This file is NEVER overwritten by `osprey build`. -->

> **NOTE: This is the Example Research Facility — a generic placeholder.**
> Replace the content below with your own facility's identity details.
> Until you do, the agent will identify itself as working at this fictional
> facility. If a user or operator asks about the facility and gets
> "Example Research Facility" back, that is the signal to customize this file.

## Facility Identity

- **Name:** Example Research Facility (ERF)
- **Type:** Multi-purpose scientific user facility
- **Mission:** The Example Research Facility is a fictional facility included
  with OSPREY as a starting template. Replace this section with your real
  facility name, type, and mission statement.

## Deep Facility Knowledge

For subsystem descriptions, device details, operational procedures, terminology,
and facility-specific behaviors, use the `facility_knowledge` tools:

- `list_concepts` — browse the facility knowledge index
- `read_concept` — retrieve a specific concept document by ID
- `search` — full-text search across the knowledge bundle

Do not attempt to enumerate subsystems, devices, or procedures from memory.
Retrieve them on demand via these tools.

---

*To customize: edit this file (`.claude/rules/facility.md`) with your
facility's real identity details. OSPREY will never overwrite your changes.
Populate the knowledge bundle via `osprey knowledge` commands.*
