# {{ project_name }}

{{ project_description }}

## Quick Start

```bash
# This project's .env was derived from the build profile that produced it.
# To add or change an API key, edit the profile's .env and rebuild — a value
# set there survives every rebuild. See .env.example for the full variable list.

# Install dependencies
uv sync

# Run the interactive CLI
osprey chat
```

## Project Structure

```
{{ project_name }}/
├── {{ package_name }}/        # Application code
│   ├── registry.py
│   ├── capabilities/
│   └── context_classes.py
├── config.yml                 # Configuration
└── pyproject.toml            # Dependencies
```

## Development

Edit files in `{{ package_name }}/` to add functionality. Changes are reflected immediately.

## Documentation

- Framework: https://als-apg.github.io/osprey
- How-to guides and reference: [OSPREY documentation](https://als-apg.github.io/osprey/)
