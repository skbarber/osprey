
services:

  nginx:
    image: nginx:1.27-alpine
    container_name: dls-nginx
    restart: unless-stopped
    network_mode: host
    labels:
      com.osprey.repo-id: "@REPO_ID@"
    environment:
      - TZ=America/Los_Angeles
      - NGINX_ENVSUBST_FILTER=^OSPREY_TERMINAL_SECRET_
      - NGINX_ENVSUBST_OUTPUT_DIR=/etc/nginx/osprey
      - OSPREY_TERMINAL_SECRET_ALICE=${OSPREY_TERMINAL_SECRET_ALICE:-}
      - OSPREY_TERMINAL_SECRET_BOB=${OSPREY_TERMINAL_SECRET_BOB:-}
    volumes:
      - ./build/nginx/nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - ./build/nginx/landing.html:/usr/share/nginx/html/landing.html:ro
      - ./build/nginx/templates:/etc/nginx/templates:ro
    tmpfs:
      - /etc/nginx/osprey:mode=0700
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://127.0.0.1:9080/ || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 10s



  web-alice:
    image: git.dls.example.org:5050/physics/production/dls-profiles/web-terminal:latest
    container_name: dls-web-alice
    restart: unless-stopped
    network_mode: host
    labels:
      com.osprey.repo-id: "@REPO_ID@"
    env_file: .env.users
    environment:
      - CLAUDE_CONFIG_DIR=/data/claude-config
      - HOME=/data/claude-config
      - TZ=America/Los_Angeles
      - OSPREY_TERMINAL_USER=alice
      - OSPREY_TERMINAL_LANDING_URL=http://dls-deploy.dls.example.org:9080
      - OSPREY_TERMINAL_BIND_HOST=127.0.0.1
      - OSPREY_TERMINAL_WEB_PORT=9100
      - OSPREY_WEB_PORT=9100
      - OSPREY_OTEL_OPENOBSERVE_HOST=127.0.0.1
      - OSPREY_TERMINAL_SECRET=${OSPREY_TERMINAL_SECRET_ALICE:-}
      - OSPREY_TERMINAL_EXTERNAL_ORIGIN=http://dls-deploy.dls.example.org:9080
      - ZO_INGEST_SA_TOKEN=${ZO_INGEST_SA_TOKEN:-}
      - OSPREY_ARTIFACT_SERVER_PORT=9200
      - OSPREY_ARIEL_PORT=9300
      - OSPREY_CHANNEL_FINDER_PORT=9500
      - OSPREY_LATTICE_DASHBOARD_PORT=9400
      - OSPREY_FACILITY_KNOWLEDGE_PORT=9600
      - OSPREY_HEALTH_PORT=9700
    volumes:
      - alice-claude-config:/data/claude-config
      - alice-agent-data:/app/dls-assistant/var/agent_data
      # This user's refusal audit log (`var/audit/<user>/` on the host), bound
      # so the record survives a recreate and is readable from the host.
      - ./var/audit/alice:/app/dls-assistant/var/audit
    healthcheck:
      # osprey web's own default listen port is the fixed constant 8087
      # (cli/web_cmd.py) — only relevant to a bare `osprey web` with no
      # OSPREY_WEB_PORT override. Every per-user container here sets
      # OSPREY_WEB_PORT above, so the probe targets the ACTUAL bound port,
      # never the bare 8087 default. Probed via bind_host itself (not a
      # hardcoded "127.0.0.1" literal) since that's the same loopback
      # address OSPREY_TERMINAL_BIND_HOST bakes into the app's own bind.
      test: ["CMD-SHELL", "curl -fsS http://127.0.0.1:9100/health || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 20s



  web-bob:
    image: git.dls.example.org:5050/physics/production/dls-profiles/web-terminal:latest
    container_name: dls-web-bob
    restart: unless-stopped
    network_mode: host
    labels:
      com.osprey.repo-id: "@REPO_ID@"
    env_file: .env.users
    environment:
      - CLAUDE_CONFIG_DIR=/data/claude-config
      - HOME=/data/claude-config
      - TZ=America/Los_Angeles
      - OSPREY_TERMINAL_USER=bob
      - OSPREY_TERMINAL_LANDING_URL=http://dls-deploy.dls.example.org:9080
      - OSPREY_TERMINAL_BIND_HOST=127.0.0.1
      - OSPREY_TERMINAL_WEB_PORT=9101
      - OSPREY_WEB_PORT=9101
      - OSPREY_OTEL_OPENOBSERVE_HOST=127.0.0.1
      - OSPREY_TERMINAL_SECRET=${OSPREY_TERMINAL_SECRET_BOB:-}
      - OSPREY_TERMINAL_EXTERNAL_ORIGIN=http://dls-deploy.dls.example.org:9080
      - ZO_INGEST_SA_TOKEN=${ZO_INGEST_SA_TOKEN:-}
      - OSPREY_ARTIFACT_SERVER_PORT=9201
      - OSPREY_ARIEL_PORT=9301
      - OSPREY_CHANNEL_FINDER_PORT=9501
      - OSPREY_LATTICE_DASHBOARD_PORT=9401
      - OSPREY_FACILITY_KNOWLEDGE_PORT=9601
      - OSPREY_HEALTH_PORT=9701
    volumes:
      - bob-claude-config:/data/claude-config
      - bob-agent-data:/app/dls-assistant/var/agent_data
      # This user's refusal audit log (`var/audit/<user>/` on the host), bound
      # so the record survives a recreate and is readable from the host.
      - ./var/audit/bob:/app/dls-assistant/var/audit
    healthcheck:
      # osprey web's own default listen port is the fixed constant 8087
      # (cli/web_cmd.py) — only relevant to a bare `osprey web` with no
      # OSPREY_WEB_PORT override. Every per-user container here sets
      # OSPREY_WEB_PORT above, so the probe targets the ACTUAL bound port,
      # never the bare 8087 default. Probed via bind_host itself (not a
      # hardcoded "127.0.0.1" literal) since that's the same loopback
      # address OSPREY_TERMINAL_BIND_HOST bakes into the app's own bind.
      test: ["CMD-SHELL", "curl -fsS http://127.0.0.1:9101/health || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 20s



volumes:

  alice-claude-config:
    labels:
      com.osprey.repo-id: "@REPO_ID@"
  alice-agent-data:
    labels:
      com.osprey.repo-id: "@REPO_ID@"

  bob-claude-config:
    labels:
      com.osprey.repo-id: "@REPO_ID@"
  bob-agent-data:
    labels:
      com.osprey.repo-id: "@REPO_ID@"

